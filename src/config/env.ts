// Environment configuration for demo/production mode
// Set to true to force mock authentication and services
export const IS_DEMO = true;

// Can be overridden by environment variable in the future
// export const IS_DEMO = import.meta.env.VITE_APP_MODE === 'mock';
